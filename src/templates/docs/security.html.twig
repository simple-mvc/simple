{% extends 'docs.html.twig' %}

{% block title %}
    {{ page }}
{% endblock %}

{% block content %}
    <div class="app-dashboard-body-content off-canvas-content" data-off-canvas-content>

        <header>
            <h1 class="text-center">Security</h1>
            <hr>

            <div class="grid-x align-center margin-top-2">
                <div class="medium-10 large-9">
                    <nav class="grid-x align-justify margin-bottom-2">
                        <a href="/docs/database"><i class="fas fa-arrow-left"></i> Database</a>
                        <a href="/docs/assets"><i class="fas fa-arrow-right"></i> Assets</a>
                    </nav>
                    <p class="lead margin-top-1 margin-bottom-0">As long as we are developing locally one our machines, threats are reasonable. But we want to conquer the world, aren’t we? So let’s say a few words about security.</p>
                </div>
            </div>
        </header>

        <div class="grid-x align-center margin-top-2">
            <div class="small-11 medium-10 large-9">

                <section>
                    <h2>Cross-site scripting (XSS)</h2>
                    <p>No parameters in GET requests.</p>
                    <p>User input systematically sanitized with a clean() helper function.</p>
                </section>

                <section>
                    <h2>SQL Injection Attacks</h2>
                    <p>Only prepared statements.</p>
                </section>

                <section>
                    <h2>Cross site request forgery (CSRF)</h2>
                    <p>A token is created for each session. When a form is submitted, the app checks if there is a match between the tokens. If not, data is not persisted.</p>
                </section>

                <section>
                    <h2>Architecture</h2>
                    <p>The visitors only access to the <code>public/</code> folder.</p>
                </section>


                <section>
                    <h2>Routes</h2>
                    <p>Every administration action is secured by a username/password login system.</p>
                </section>

                <section>
                    <h2>Uploads</h2>
                    <p>Files uploaded are multiple checked.</p>
                </section>

                <nav class="grid-x align-justify padding-bottom-2">
                    <a href="/docs/database"><i class="fas fa-arrow-left"></i> Database</a>
                    <a href="/docs/assets"><i class="fas fa-arrow-right"></i> Assets</a>
                </nav>

            </div>
        </div>
    </div>
{% endblock %}